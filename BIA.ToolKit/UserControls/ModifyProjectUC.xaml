<UserControl x:Class="BIA.ToolKit.UserControls.ModifyProjectUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:uc="clr-namespace:BIA.ToolKit.UserControls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:Height="550" d:Width="1280">
    <Grid x:Name="TabModify">
        <Grid Margin="10,0,10,0">
            <Grid Background="{DynamicResource MaterialDesign.Brush.Background}" Opacity="0.2"/>
            <TabControl x:Name="TabActions" SelectedIndex="{Binding SelectedTabIndex}" IsEnabled="{Binding IsProjectSelected}">
                <TabItem x:Name="TabProject" Header="Project" >
                    <Grid Margin="5" IsEnabled="{Binding IsProjectSelected}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Background="{DynamicResource MaterialDesign.Brush.Background}" Opacity="0.2" Margin="-5" Grid.ColumnSpan="2"/>
                        <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto" Height="auto">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="auto"/>
                                </Grid.RowDefinitions>
                                <GroupBox Header="Original" Margin="10" x:Name="CompanyFileGroup" Grid.Row="0" Style="{StaticResource MaterialDesignGroupBox}" materialDesign:ColorZoneAssist.Mode="Custom" materialDesign:ColorZoneAssist.Background="{DynamicResource MaterialDesign.Brush.Primary}" materialDesign:ColorZoneAssist.Foreground="White">
                                    <uc:VersionAndOptionUserControl x:Name="MigrateOriginVersionAndOption"/>
                                </GroupBox>
                                <GroupBox Margin="10" Header="Target" Grid.Row="1" Style="{StaticResource MaterialDesignGroupBox}" materialDesign:ColorZoneAssist.Mode="Custom" materialDesign:ColorZoneAssist.Background="{DynamicResource MaterialDesign.Brush.Primary}" materialDesign:ColorZoneAssist.Foreground="White">
                                    <uc:VersionAndOptionUserControl x:Name="MigrateTargetVersionAndOption"/>
                                </GroupBox>
                            </Grid>
                        </ScrollViewer>
                        <Grid Grid.Column="1" Margin="15,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <CheckBox Grid.Row="0" Content="Overwrite BIA first" HorizontalAlignment="Center" Name="CFOverwriteBIAFirst" IsChecked="{Binding Path=OverwriteBIAFromOriginal, Mode=TwoWay}"/>
                            <Button Style="{StaticResource MaterialDesignRaisedSecondaryButton}" Margin="0,10" Height="50" Grid.Row="1" HorizontalAlignment="Stretch" Content="Migrate" x:Name="MigrateBtn" Command="{Binding MigrateCommand}" IsEnabled="{Binding IsProjectSelected}"/>
                            <Label Grid.Row="2" Content="Or by step :" HorizontalAlignment="Center"/>
                            <StackPanel Grid.Row="3" Margin="0,10">
                                <Button Margin="0,3" Content="1 - Generate Only" Command="{Binding MigrateGenerateOnlyCommand}" IsEnabled="{Binding IsProjectSelected}"/>
                                <Button Margin="0,3" Content="2 - Open Folder" Command="{Binding MigrateOpenFolderCommand}" IsEnabled="{Binding CanOpenFolder}"/>
                                <Button Margin="0,3" Content="3 - Apply Diff" Command="{Binding MigrateApplyDiffCommand}" IsEnabled="{Binding CanApplyDiff}"/>
                                <Button Margin="0,3" Content="4 - Merge Rejected" Command="{Binding MigrateMergeRejectedCommand}" IsEnabled="{Binding CanMergeRejected}"/>
                                <Button Margin="0,3" Content="5 - Overwrite '\BIA-.*'" Command="{Binding MigrateOverwriteBIAFolderCommand}" IsEnabled="{Binding IsProjectSelected}"/>
                                <Button Margin="0,3" Content="6 - Fix Usings" Command="{Binding FixUsingsCommand}" IsEnabled="{Binding IsProjectSelected}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </TabItem>
                <TabItem x:Name="TabRegenerateFeatures" Header="Features"
                         IsEnabled="{Binding IsTabFeaturesEnabled}">
                    <uc:RegenerateFeaturesUC x:Name="RegenerateFeatures"/>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>

</UserControl>
