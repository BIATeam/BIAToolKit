<UserControl xmlns:UserControls="clr-namespace:BIA.ToolKit.UserControls"  x:Class="BIA.ToolKit.UserControls.VersionAndOptionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BIA.ToolKit.Application.ViewModel;assembly=BIA.ToolKit.Application"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignWidth="800" Height="250">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>
    <UserControl.DataContext>
        <!-- Declaratively create an instance of our SongViewModel -->
        <local:VersionAndOptionViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="0.6*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ComboBox Grid.Column="0" ItemsSource="{Binding WorkTemplates}" 
                  DisplayMemberPath="Version"
                  SelectedItem="{Binding WorkTemplate}"
                  SelectionChanged="FrameworkVersion_SelectionChanged"
                  Style="{StaticResource MaterialDesignFilledComboBox}"
                  materialDesign:HintAssist.Hint="Framework Version" VerticalAlignment="Top"/>

        <Image Grid.Column="1" Grid.ColumnSpan="2" gif:ImageBehavior.AnimatedSource="..\Images\spinner_light.gif"  d:Visibility="Visible" Visibility ="{Binding Path=AreFeatureLoading, Converter={StaticResource BoolToVisConverter} }"/>

        <GroupBox Margin="10,0" Grid.Column="1" Header="Feature Settings" Foreground="White" Name="FeatureSettingsGroup" d:Visibility="Visible" Visibility ="{Binding Path=AreFeatureVisible, Converter={StaticResource BoolToVisConverter} }" VerticalAlignment="Top" Background="#323232">
            <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto" Height="auto">
                <ItemsControl Margin="0,0,10,0" ItemsSource="{Binding FeatureSettings}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsSelected}" ToolTip="{Binding Description}" Foreground="White" Margin="0,5" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <GroupBox Grid.Column="2" Header="Company files" Foreground="White" Name="CompanyFileGroup" Background="#323232" VerticalAlignment="Top">
            <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto" Height="auto">
                <Grid Margin="0,0,10,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <CheckBox Grid.Row="0" Margin="0,5" IsChecked="{Binding UseCompanyFiles}" IsEnabled="{Binding NotUseCompanyFiles, Converter={StaticResource BoolToVisConverter}}" Name="UseCompanyFiles" Content="Use Company Files" Foreground="White" Checked="UseCompanyFile_Checked" Unchecked="UseCompanyFile_Checked" />
                    <StackPanel Grid.Row="1" Grid.RowSpan="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Visibility="{Binding NotUseCompanyFiles, Converter={StaticResource BoolToVisConverter}}" Name="Warning" Source="..\Images\Warning.png" HorizontalAlignment="Left" Height="50"  VerticalAlignment="Top" Width="50"/>
                        <Label Visibility="{Binding NotUseCompanyFiles, Converter={StaticResource BoolToVisConverter}}" Name="WarningLabel"  Content="You haven't select Company Files" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="White" FontSize="20"/>
                    </StackPanel>
                    <ComboBox Grid.Row="1" Margin="0,5"
                    ItemsSource="{Binding WorkCompanyFiles}" 
                    DisplayMemberPath="Version"
                    SelectedItem="{Binding WorkCompanyFile}"
                    SelectionChanged="CFVersion_SelectionChanged"
                    Visibility="{Binding UseCompanyFiles, Converter={StaticResource BoolToVisConverter}}"
                    Style="{StaticResource MaterialDesignFilledComboBox}"
                    materialDesign:HintAssist.Hint="Company File Version" />

                    <Grid Grid.Row="2" Margin="0,5,0,0" Visibility="{Binding UseCompanyFiles, Converter={StaticResource BoolToVisConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <ComboBox Grid.Row="0" ItemsSource="{Binding Profiles}" SelectedItem="{Binding Profile}" 
                          Style="{StaticResource MaterialDesignFilledComboBox}"
                          materialDesign:HintAssist.Hint="Profile"/>

                        <ItemsControl Margin="0,5" Grid.Row="1" ItemsSource="{Binding Options}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}" HorizontalAlignment="Left" VerticalAlignment="Top" Foreground="White" Margin="0,5" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Grid>
            </ScrollViewer>
        </GroupBox>
        <Canvas/>
    </Grid>
</UserControl>
