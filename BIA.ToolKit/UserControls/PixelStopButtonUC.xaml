<UserControl x:Class="BIA.ToolKit.UserControls.PixelStopButtonUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="48" d:DesignWidth="160">
    <Border x:Name="HitArea" Background="Transparent" Cursor="Hand"
            MouseLeftButtonDown="OnClick" MouseEnter="OnMouseEnter" MouseLeave="OnMouseLeave">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Canvas x:Name="StopCanvas" Width="40" Height="40" ClipToBounds="False"/>
            <TextBlock x:Name="LabelText" Text="Annuler" FontSize="16" FontWeight="Medium"
                       Foreground="IndianRed" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
