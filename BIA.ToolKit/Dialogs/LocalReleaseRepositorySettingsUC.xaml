<Window x:Class="BIA.ToolKit.Dialogs.LocalReleaseRepositorySettingsUC"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BIA.ToolKit.Dialogs"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:BIA.ToolKit.Application.ViewModel;assembly=BIA.ToolKit.Application"
        mc:Ignorable="d"
        Title="Local Release Repository Settings" Height="Auto" Width="500"
        MinWidth="500"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}">
    <Window.DataContext>
        <vm:LocalReleaseRepositorySettingsViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <CheckBox Margin="5" Grid.Row="0" IsChecked="{Binding UseLocalReleaseRepository}" Content="Use a local release repository"/>
        <TextBox Margin="5" Grid.Row="1" Style="{StaticResource MaterialDesignFilledTextBox}" materialDesign:HintAssist.Hint="Local Release Repository" Text="{Binding LocalReleaseRepositoryPath}" IsEnabled="{Binding UseLocalReleaseRepository}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="5" Name="OkButton" Click="OkButton_Click" IsDefault="True">OK</Button>
            <Button Margin="5" Name="CancelButton" Click="CancelButton_Click" IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
