<Window x:Class="BIA.ToolKit.Dialogs.LogDetailUC"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:BIA.ToolKit.Application.ViewModel;assembly=BIA.ToolKit.Application"
        Title="Log Detail"
        Width="300"
        Height="200"
        MinWidth="600"
        MinHeight="500"
        MaxWidth="1035"
        MaxHeight="765"
        d:DataContext="{d:DesignInstance Type=viewModel:LogDetailViewModel,
                                         IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Style="{StaticResource MaterialDesignWindow}"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer x:Name="OutputDetailTextViewer" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="OutputDetailText"
                       Padding="5"
                       Background="#1a1a1a"
                       LineHeight="10"
                       TextWrapping="Wrap" />
        </ScrollViewer>

        <Button x:Name="copyToClipboard"
                Grid.Row="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Command="{Binding CopyToClipboardCommand}"
                Content="Copy to clipboard"
                Style="{StaticResource MaterialDesignOutlinedButton}" />
    </Grid>

</Window>
