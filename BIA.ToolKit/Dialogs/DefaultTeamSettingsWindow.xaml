<Window x:Class="BIA.ToolKit.Dialogs.DefaultTeamSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:BIA.ToolKit.Application.ViewModel;assembly=BIA.ToolKit.Application"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Default Team Settings"
        MinWidth="400"
        MaxWidth="400"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}"
        d:DataContext="{d:DesignInstance Type=viewModel:DefaultTeamSettingsViewModel, IsDesignTimeCreatable=False}">
    <Window.Resources>
        <Thickness x:Key="FormFieldMargin">0,0,0,8</Thickness>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBox Margin="{StaticResource FormFieldMargin}"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     materialDesign:HintAssist.Hint="Team Name (Singular)"
                     Text="{Binding DefaultTeamName, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"/>
            <TextBox Margin="{StaticResource FormFieldMargin}"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     materialDesign:HintAssist.Hint="Team Name (Plural)"
                     Text="{Binding DefaultTeamNamePlural, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"/>
            <TextBox Margin="{StaticResource FormFieldMargin}"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     materialDesign:HintAssist.Hint="Domain Name"
                     Text="{Binding DefaultTeamDomainName, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Margin="0,5,0,0" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="5" x:Name="SubmitButton" IsDefault="True" Click="SubmitButton_Click"
                    IsEnabled="{Binding IsValid}">OK</Button>
            <Button Margin="5" IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
